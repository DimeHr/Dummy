import React from 'react';
import { Button } from './styles'

const ButtonMain = (props) => {
    return (
        <Button {...props}>
            my button
        </Button>

    )
};

export default ButtonMain;